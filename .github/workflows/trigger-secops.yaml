name: Application to SecOps Trigger

on:
  push:
    branches:
      - main

jobs:
  application-to-secops:
    name: Verify Application and Trigger SecOps
    runs-on: ubuntu-latest

    steps:
      - name: Step 1 - Verify application push
        run: |
          echo "Application repository push"
          echo "Repository: ${{ github.repository }}"
          echo "Branch: ${{ github.ref_name }}"
          echo "Commit SHA: ${{ github.sha }}"
